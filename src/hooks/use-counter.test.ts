import { useCounter } from './use-counter'
import { renderHook } from '@testing-library/react-hooks'

// Only test custom hooks directly if they are more global
// or used accross the application.

describe('useCounter', () => {

})